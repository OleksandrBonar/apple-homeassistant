kitchen_sensor:
  sensor:
    - platform: history_stats
      name: Kitchen - Sensor - Light On - Duration - Today
      id: kitchen_sensor_light_on_duration_today
      entity_id: light.kitchen
      state: "on"
      type: time
      start: "{{ now().replace(hour=0, minute=0, second=0) }}"
      end: "{{ now() }}"
    - platform: history_stats
      name: Kitchen - Sensor - Window Close - Duration - Last Two Hourse
      id: kitchen_sensor_window_close_duration_last_two_hours
      entity_id: binary_sensor.snzb_04_02_contact
      state: "off"
      type: time
      end: "{{ now() }}"
      duration:
        hours: 2
